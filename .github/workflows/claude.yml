name: Claude Code
on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
permissions:
  contents: write
  pull-requests: write
  issues: write
  id-token: write
jobs:
  review:
    uses: Round2POS/.github/.github/workflows/claude-review.yml@main
    secrets: inherit
